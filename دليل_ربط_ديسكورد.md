# 🔗 دليل ربط الاستمارة مع Discord

## 📋 الخطوات التفصيلية

### الخطوة 1: إنشاء قناة في Discord

1. افتح تطبيق **Discord** (على الكمبيوتر أو الموبايل)
2. اختر السيرفر (Server) الذي تريد استخدامه
3. أنشئ قناة جديدة أو استخدم قناة موجودة
   - اضغط بزر الماوس الأيمن على قائمة القنوات
   - اختر **Create Channel**
   - اختر اسم للقناة (مثلاً: `gym-registrations` أو `تسجيلات-الجيم`)

### الخطوة 2: إنشاء Webhook

1. **اضغط بزر الماوس الأيمن** على القناة التي أنشأتها
2. اختر **Edit Channel** (تعديل القناة)
3. من القائمة الجانبية، اختر **Integrations** (التكاملات)
4. اضغط على **Webhooks** من القائمة العلوية
5. اضغط على زر **Create Webhook** أو **New Webhook**

### الخطوة 3: إعداد Webhook

1. **اختر اسم للـ Webhook** (مثلاً: `Gym Registration Bot`)
2. **اختر صورة** (اختياري) - يمكنك اختيار صورة للبوت
3. **اختر القناة** - تأكد أنها القناة الصحيحة
4. اضغط على **Copy Webhook URL** (نسخ رابط الـ Webhook)
   - الرابط سيبدو هكذا:
   ```
   https://discord.com/api/webhooks/1234567890123456789/abcdefghijklmnopqrstuvwxyz1234567890
   ```
5. اضغط **Save Changes** (حفظ التغييرات)

### الخطوة 4: إضافة الرابط إلى المشروع

1. افتح ملف **`config.js`** في المشروع
2. ابحث عن السطر:
   ```javascript
   const DISCORD_WEBHOOK_URL = 'YOUR_WEBHOOK_URL_HERE';
   ```
3. استبدل `YOUR_WEBHOOK_URL_HERE` برابط الـ Webhook الذي نسخته
4. يجب أن يصبح السطر هكذا:
   ```javascript
   const DISCORD_WEBHOOK_URL = 'https://discord.com/api/webhooks/1234567890/abcdefghijklmnopqrstuvwxyz';
   ```
5. احفظ الملف (Ctrl + S)

### الخطوة 5: اختبار الربط

1. افتح ملف **`index.html`** في المتصفح
2. املأ الاستمارة ببيانات تجريبية
3. اضغط **إرسال التسجيل**
4. اذهب إلى قناة Discord
5. يجب أن تظهر رسالة منظمة تحتوي على بيانات المشترك

## ✅ مثال على الرسالة في Discord

عند إرسال الاستمارة، ستظهر رسالة في Discord بهذا الشكل:

```
🎯 تسجيل جديد في الجيم

👤 الاسم الكامل: أحمد محمد
📱 رقم الجوال: 0512345678
🎂 العمر: 25
📅 نوع الاشتراك: شهري
🎯 هدف التمرين: بناء العضلات
💪 مدرب شخصي: نعم

تم التسجيل في 15 يناير 2024، 10:30 ص
```

## 🐛 حل المشاكل

### المشكلة: لا تظهر الرسائل في Discord

**الحل:**
1. تحقق من صحة رابط الـ Webhook (تأكد أنه مكتمل)
2. تأكد من أن الـ Webhook لم يتم حذفه
3. افتح Console في المتصفح (اضغط F12) للتحقق من الأخطاء
4. تأكد من أن القناة موجودة ولديك صلاحيات الكتابة فيها

### المشكلة: رسالة خطأ "CORS"

**الحل:**
- استخدم خادم محلي بدلاً من فتح الملف مباشرة
- استخدم أحد الأوامر التالية:

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js
npx http-server

# PHP
php -S localhost:8000
```

ثم افتح: `http://localhost:8000`

### المشكلة: رابط الـ Webhook لا يعمل

**الحل:**
1. اذهب إلى إعدادات القناة → Integrations → Webhooks
2. تأكد من أن الـ Webhook موجود ومفعل
3. إذا لزم الأمر، أنشئ Webhook جديد وانسخ الرابط مرة أخرى

## 🔒 الأمان

⚠️ **مهم جداً:**

- **لا تشارك رابط الـ Webhook** مع أي شخص
- **لا ترفع ملف config.js** على GitHub أو أي منصة عامة
- إذا أردت النشر على GitHub:
  - استخدم متغيرات البيئة (Environment Variables)
  - أو استخدم Backend بسيط لإخفاء الرابط

## 📸 صور توضيحية (خطوات Discord)

### الخطوة 1: Edit Channel
```
القناة → بزر الماوس الأيمن → Edit Channel
```

### الخطوة 2: Integrations
```
Edit Channel → Integrations (من القائمة الجانبية)
```

### الخطوة 3: Webhooks
```
Integrations → Webhooks (من القائمة العلوية)
```

### الخطوة 4: Create Webhook
```
Webhooks → Create Webhook → Copy Webhook URL
```

## 🎉 مبروك!

بعد اتباع هذه الخطوات، ستكون الاستمارة مربوطة مع Discord وستستقبل جميع التسجيلات تلقائياً!

---

**ملاحظة:** إذا واجهت أي مشكلة، تأكد من:
1. صحة رابط الـ Webhook
2. أن الـ Webhook لم يتم حذفه
3. أن لديك صلاحيات في القناة
4. فتح Console في المتصفح للتحقق من الأخطاء

