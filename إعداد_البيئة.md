# 🔐 إعداد ملف .env

## خطوات إنشاء ملف .env

### 1. إنشاء الملف

أنشئ ملف جديد باسم `.env` في المجلد الرئيسي للمشروع (نفس مكان `index.html`).

### 2. إضافة المحتوى

افتح ملف `.env` وأضف السطر التالي:

```env
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1451095370452176906/c-PL2Z8S3hm4ky5z1-clAHSQ1i5lMqMerurOXutEnGD5sIfN42fEkVkJI7-9LyWUqgcO
```

### 3. حفظ الملف

احفظ الملف باسم `.env` (بدون أي امتداد).

## 📝 مثال على ملف .env

```env
# Discord Webhook URL
# ⚠️ هذا الملف محمي ولا يتم رفعه على GitHub

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1451095370452176906/c-PL2Z8S3hm4ky5z1-clAHSQ1i5lMqMerurOXutEnGD5sIfN42fEkVkJI7-9LyWUqgcO
```

## ✅ التحقق

1. تأكد من وجود ملف `.env` في المجلد الرئيسي
2. تأكد من أن الرابط موجود في الملف
3. افتح `index.html` في المتصفح
4. افتح Console (F12) - يجب أن ترى: "تم تحميل متغيرات البيئة من ملف .env بنجاح"

## 🔒 الأمان

- ✅ ملف `.env` موجود في `.gitignore` (لن يتم رفعه على GitHub)
- ✅ يمكنك رفع المشروع على GitHub بأمان
- ⚠️ لا ترفع ملف `.env` يدوياً على GitHub

## 🐛 حل المشاكل

### الملف لا يعمل؟
- تأكد من أن اسم الملف هو `.env` (بدون امتداد)
- تأكد من أن الملف في المجلد الرئيسي
- تأكد من عدم وجود مسافات إضافية في الرابط

### رسالة خطأ في Console؟
- افتح Console (F12) للتحقق من الأخطاء
- تأكد من أن الملف موجود ويمكن قراءته

---

**ملاحظة**: يمكنك استخدام ملف `.env.example` كقالب - انسخه إلى `.env` واملأ القيم.

