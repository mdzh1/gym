# 🏋️ استمارة تسجيل الجيم

استمارة تسجيل احترافية ومتجاوبة للجيم مع إرسال إشعارات تلقائية إلى Discord عبر Webhook.

## ✨ المميزات

- ✅ تصميم متجاوب يعمل على جميع الأجهزة
- ✅ التحقق من صحة البيانات قبل الإرسال
- ✅ إرسال إشعارات تلقائية إلى Discord مع Embed احترافي
- ✅ حفظ البيانات محلياً (LocalStorage)
- ✅ واجهة مستخدم عصرية وجذابة
- ✅ دعم اللغة العربية بالكامل

## 📋 الحقول المطلوبة

1. **الاسم الكامل** - التحقق من وجود 3 أحرف على الأقل
2. **رقم الجوال** - التحقق من صحة الرقم السعودي (10 أرقام)
3. **العمر** - من 16 إلى 100 سنة
4. **نوع الاشتراك** - يومي، أسبوعي، شهري، ربع سنوي، نصف سنوي، سنوي
5. **هدف التمرين** - بناء العضلات، تنشيف، فقدان الوزن، إلخ
6. **مدرب شخصي** - نعم / لا

## 🚀 البدء السريع

### 1. إعداد Discord Webhook

1. افتح تطبيق Discord
2. اذهب إلى القناة التي تريد استقبال الإشعارات فيها
3. اضغط بزر الماوس الأيمن على القناة → **Edit Channel**
4. اختر **Integrations** من القائمة الجانبية
5. اضغط على **Webhooks** → **New Webhook**
6. اختر اسم للـ Webhook (مثلاً: Gym Registration Bot)
7. انسخ رابط الـ Webhook (Copy Webhook URL)

### 2. إعداد المشروع للتطوير المحلي

1. أنشئ ملف `.env` في المجلد الرئيسي:
   ```env
   DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1234567890/abcdefghijklmnopqrstuvwxyz
   ```

2. شغّل السكريبت لإنشاء ملف الإعدادات:
   ```bash
   node create-env-config.js
   ```

3. افتح `index.html` في المتصفح

### 3. النشر على Vercel

1. ارفع المشروع إلى GitHub
2. اربط المشروع مع Vercel
3. في **Project Settings** → **Environment Variables**:
   - أضف متغير: `DISCORD_WEBHOOK_URL`
   - القيمة: رابط الـ Webhook الخاص بك
4. Vercel سيستخدم `/api/env` تلقائياً

## 📁 هيكل المشروع

```
gym/
├── index.html              # الصفحة الرئيسية
├── styles.css              # ملف التصميم
├── script.js               # منطق الاستمارة
├── load-env.js            # تحميل متغيرات البيئة
├── config.js              # تهيئة الإعدادات
├── create-env-config.js   # سكريبت لإنشاء env-config.js
├── env-config.js          # ملف الإعدادات (يتم إنشاؤه من .env)
├── .env                   # ملف البيئة (محمي - لا يتم رفعه)
├── api/
│   └── env.js             # Vercel Serverless Function
└── emge/
    └── نادي.png           # شعار النادي
```

## 🔒 الأمان

- ✅ ملف `.env` محمي في `.gitignore`
- ✅ ملف `env-config.js` محمي في `.gitignore`
- ✅ يمكن رفع المشروع على GitHub بأمان
- ⚠️ لا ترفع ملفات `.env` أو `env-config.js` يدوياً

## 🐛 حل المشاكل

### المشكلة: لا تظهر الإشعارات في Discord

1. تحقق من صحة رابط الـ Webhook
2. تأكد من أن الـ Webhook لم يتم حذفه
3. افتح Console في المتصفح (F12) للتحقق من الأخطاء

### المشكلة: رسالة خطأ "CORS"

استخدم خادم محلي:
```bash
# Python 3
python -m http.server 8000

# Node.js
npx http-server
```

ثم افتح: `http://localhost:8000`

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**صُنع بـ ❤️ للمجتمع العربي**
