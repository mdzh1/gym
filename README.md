# 🏋️ استمارة تسجيل الجيم

استمارة تسجيل احترافية ومتجاوبة للجيم مع إرسال إشعارات تلقائية إلى Discord عبر Webhook.

## ✨ المميزات

- ✅ تصميم متجاوب يعمل على جميع الأجهزة
- ✅ التحقق من صحة البيانات قبل الإرسال
- ✅ إرسال إشعارات تلقائية إلى Discord مع Embed احترافي
- ✅ حفظ البيانات محلياً (LocalStorage)
- ✅ واجهة مستخدم عصرية وجذابة
- ✅ دعم اللغة العربية بالكامل

## 📋 الحقول المطلوبة

1. **الاسم الكامل** - التحقق من وجود 3 أحرف على الأقل
2. **رقم الجوال** - التحقق من صحة الرقم السعودي (10 أرقام)
3. **العمر** - من 16 إلى 100 سنة
4. **نوع الاشتراك** - يومي، أسبوعي، شهري، ربع سنوي، نصف سنوي، سنوي
5. **هدف التمرين** - بناء العضلات، تنشيف، فقدان الوزن، إلخ
6. **مدرب شخصي** - نعم / لا

## 🚀 البدء السريع

### 1. إعداد Discord Webhook

1. افتح تطبيق Discord
2. اذهب إلى القناة التي تريد استقبال الإشعارات فيها
3. اضغط بزر الماوس الأيمن على القناة → **Edit Channel**
4. اختر **Integrations** من القائمة الجانبية
5. اضغط على **Webhooks** → **New Webhook**
6. اختر اسم للـ Webhook (مثلاً: Gym Registration Bot)
7. انسخ رابط الـ Webhook (Copy Webhook URL)

### 2. إعداد المشروع

1. افتح ملف `config.js`
2. استبدل `YOUR_WEBHOOK_URL_HERE` برابط الـ Webhook الذي نسخته:

```javascript
const DISCORD_WEBHOOK_URL = 'https://discord.com/api/webhooks/1234567890/abcdefghijklmnopqrstuvwxyz';
```

### 3. تشغيل المشروع

#### الطريقة الأولى: فتح الملف مباشرة
- افتح ملف `index.html` في المتصفح

#### الطريقة الثانية: استخدام خادم محلي

**باستخدام Python:**
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

**باستخدام Node.js:**
```bash
npx http-server
```

**باستخدام PHP:**
```bash
php -S localhost:8000
```

ثم افتح المتصفح على: `http://localhost:8000`

## 📁 هيكل المشروع

```
gym/
│
├── index.html          # الصفحة الرئيسية
├── styles.css          # ملف التصميم
├── script.js           # ملف JavaScript
├── config.js           # ملف الإعدادات (Webhook URL)
└── README.md           # ملف التوثيق
```

## 🔒 الأمان

⚠️ **تحذير مهم:**

لا تضع رابط الـ Webhook مباشرة في الكود عند النشر على GitHub أو أي منصة عامة!

### الحلول المقترحة:

1. **استخدام متغيرات البيئة (Environment Variables)**
   - في Netlify: إضافة متغير في Settings → Environment Variables
   - في Vercel: إضافة متغير في Project Settings → Environment Variables

2. **استخدام Backend بسيط**
   - إنشاء API endpoint بسيط
   - إرسال البيانات من Frontend إلى Backend
   - Backend يرسل البيانات إلى Discord

3. **استخدام Serverless Function**
   - Netlify Functions
   - Vercel Functions
   - AWS Lambda

4. **إضافة config.js إلى .gitignore**
   ```gitignore
   config.js
   ```

## 🎨 التخصيص

### تغيير الألوان

في ملف `styles.css`، يمكنك تعديل المتغيرات:

```css
:root {
    --primary-color: #6366f1;
    --secondary-color: #8b5cf6;
    --success-color: #10b981;
    /* ... */
}
```

### تعديل الحقول

في ملف `index.html`، يمكنك:
- إضافة حقول جديدة
- تعديل خيارات القوائم المنسدلة
- تغيير النصوص

### تعديل رسالة Discord

في ملف `script.js`، يمكنك تعديل كائن `embed`:

```javascript
const embed = {
    title: "🎯 تسجيل جديد في الجيم",
    color: 0x6366f1,
    fields: [
        // ... الحقول
    ]
};
```

## 📱 التوافق

- ✅ Chrome / Edge (آخر إصدارين)
- ✅ Firefox (آخر إصدارين)
- ✅ Safari (آخر إصدارين)
- ✅ متصفحات الجوال (Chrome Mobile, Safari Mobile)
- ✅ جميع أحجام الشاشات (Desktop, Tablet, Mobile)

## 🌐 النشر

### Netlify

1. ارفع الملفات إلى GitHub
2. اذهب إلى [Netlify](https://www.netlify.com)
3. اضغط على **New site from Git**
4. اختر المستودع
5. في **Build settings**:
   - Build command: اتركه فارغاً
   - Publish directory: `/` أو اتركه فارغاً
6. أضف متغير البيئة `DISCORD_WEBHOOK_URL` في **Site settings → Environment variables**
7. في `config.js`، استخدم: `const DISCORD_WEBHOOK_URL = process.env.DISCORD_WEBHOOK_URL;`

### GitHub Pages

1. ارفع الملفات إلى GitHub
2. اذهب إلى **Settings** → **Pages**
3. اختر الفرع الرئيسي (main/master)
4. اضغط **Save**
5. ⚠️ **تحذير**: لا تضع رابط الـ Webhook في الكود عند النشر على GitHub Pages!

### Vercel

1. ارفع الملفات إلى GitHub
2. اذهب إلى [Vercel](https://vercel.com)
3. اضغط على **New Project**
4. اختر المستودع
5. اضغط **Deploy**
6. أضف متغير البيئة في **Project Settings → Environment Variables**

## 🐛 حل المشاكل

### لا تظهر الإشعارات في Discord

1. تحقق من صحة رابط الـ Webhook
2. تأكد من أن الـ Webhook لم يتم حذفه
3. افتح Console في المتصفح (F12) للتحقق من الأخطاء

### رسالة خطأ "CORS"

- هذا يحدث عند فتح الملف مباشرة من النظام
- استخدم خادم محلي (راجع قسم "تشغيل المشروع")

### الحقول لا تتحقق بشكل صحيح

- تأكد من أن جميع الحقول مطلوبة (required)
- تحقق من أن JavaScript يعمل بشكل صحيح

## 📝 التطوير المستقبلي

- [ ] ربط WhatsApp
- [ ] لوحة تحكم للإدارة
- [ ] نظام اشتراكات
- [ ] الدفع الإلكتروني
- [ ] حفظ البيانات في قاعدة بيانات
- [ ] إرسال نسخة عبر البريد الإلكتروني
- [ ] ربط Google Sheets

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 👨‍💻 المساهمة

نرحب بجميع المساهمات! يرجى فتح Issue أو Pull Request.

## 📞 الدعم

إذا واجهت أي مشكلة، يرجى فتح Issue في المستودع.

---

**صُنع بـ ❤️ للمجتمع العربي**

