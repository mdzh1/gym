# 🔒 دليل الأمان - إخفاء Discord Webhook

## ✅ ما تم إنجازه

تم إخفاء رابط Discord Webhook للأمان من خلال:

1. **إنشاء ملف `config.local.js`** - يحتوي على الرابط الحقيقي
2. **إضافة `config.local.js` إلى `.gitignore`** - لن يتم رفعه على GitHub
3. **تعديل `config.js`** - يحتوي على قيمة افتراضية فقط
4. **تعديل `index.html`** - يحمل `config.local.js` أولاً

## 📁 هيكل الملفات

```
gym/
├── config.js          # ملف عام (يتم رفعه على GitHub) - لا يحتوي على رابط حقيقي
├── config.local.js    # ملف محلي محمي (لا يتم رفعه) - يحتوي على الرابط الحقيقي
└── .gitignore         # يحتوي على config.local.js
```

## 🔐 كيف يعمل النظام

1. عند فتح الصفحة، يتم تحميل `config.local.js` أولاً
2. إذا كان الملف موجوداً، سيتم استخدام الرابط الحقيقي
3. إذا لم يكن موجوداً، سيتم استخدام القيمة الافتراضية من `config.js`
4. ملف `config.local.js` محمي في `.gitignore` ولن يتم رفعه على GitHub

## ⚠️ تحذيرات مهمة

### عند النشر على GitHub:
- ✅ **آمن**: ملف `config.local.js` لن يتم رفعه (موجود في `.gitignore`)
- ✅ **آمن**: ملف `config.js` لا يحتوي على رابط حقيقي
- ⚠️ **تحذير**: لا ترفع `config.local.js` يدوياً على GitHub

### عند مشاركة المشروع:
- ✅ يمكنك مشاركة المشروع بأمان
- ✅ الرابط الحقيقي محمي في `config.local.js`
- ⚠️ كل شخص يحتاج إنشاء `config.local.js` خاص به

## 📝 إنشاء config.local.js جديد

إذا أردت إنشاء ملف جديد أو على جهاز آخر:

1. انسخ ملف `config.example.js`
2. أعد تسميته إلى `config.local.js`
3. أضف رابط الـ Webhook الخاص بك:

```javascript
const DISCORD_WEBHOOK_URL = 'رابط_الويب_هوك_الخاص_بك';
```

## 🚀 النشر على منصات الاستضافة

### Netlify / Vercel:
استخدم **Environment Variables** بدلاً من `config.local.js`:

1. في إعدادات المشروع، أضف متغير:
   - **Name**: `DISCORD_WEBHOOK_URL`
   - **Value**: رابط الـ Webhook

2. عدل `config.js` لاستخدام المتغير:
```javascript
const DISCORD_WEBHOOK_URL = process.env.DISCORD_WEBHOOK_URL || 'YOUR_WEBHOOK_URL_HERE';
```

### GitHub Pages:
⚠️ **لا تستخدم GitHub Pages** مع Webhook مباشر (غير آمن)
- استخدم Netlify أو Vercel مع Environment Variables
- أو استخدم Backend بسيط

## ✅ التحقق من الأمان

1. افتح `.gitignore` وتأكد من وجود `config.local.js`
2. افتح `config.js` وتأكد أنه لا يحتوي على رابط حقيقي
3. تأكد من وجود `config.local.js` محلياً (لن يظهر على GitHub)

## 🎯 الخلاصة

- ✅ الرابط الحقيقي محمي في `config.local.js`
- ✅ `config.local.js` موجود في `.gitignore`
- ✅ يمكن رفع المشروع على GitHub بأمان
- ✅ كل شخص يحتاج إنشاء `config.local.js` خاص به

---

**ملاحظة**: هذا الحل مناسب للاستخدام المحلي والتطوير. للنشر الإنتاجي، استخدم Environment Variables أو Backend.

